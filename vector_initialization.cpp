#include<bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int>v; type 1 time=>0(1); 
    // vector<int>v(10); tipe 2 time=>0(n);
     vector<int>v (10,2); 
    for(int i=0; i<v.size(); i++)
    {
        cout<<v[i]<<endl;
    }
    //  vector<int>v2(v);    type 4
    // int a[5]={1,2,3,5,6};
    // vector <int> v(a,a+5); type 5

    // vector<int>v(10,9);
    // vector<int>v2(v);

 

    // vector <int>v={1,2,3,4};//type 6;
    //  for(int i=0; i<v.size();i++)
    //  {
    //     cout<<v[i]<<endl;
    //  }
    // cout<<v.size();
    return 0;
}